[package]
name = "tb-backup"
version = "0.1.0"
description = "ToolBox 核心插件：自动备份（vault 快照 + 配置/插件存档 + 恢复）"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
tb-sdk = { path = "../../tb-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
