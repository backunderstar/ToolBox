[package]
name = "tb-records"
version = "0.1.0"
description = "ToolBox 核心插件：工作记录（data/records/*.json CRUD + 搜索提供者）"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
tb-sdk = { path = "../../tb-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
