[package]
name = "tb-todos"
version = "0.1.0"
description = "ToolBox 核心插件：快速待办（vault/data/todos/todos.json，浮窗数据层）"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
tb-sdk = { path = "../../tb-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
