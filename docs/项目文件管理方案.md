# 项目文件管理 —— 详细设计方案（M8）

> 状态：设计待评审（未实施）
> 关联需求：#3「工作时进行项目文件管理：项目目录、归档，映射文件夹和文件，点击时像在文件夹里一样用默认应用打开」
> 本文档基于上一版概要方案细化到**可实施粒度**（API 签名 / 组件树 / 交互 / 测试 / 任务拆分）。

---

## 0. 决策记录（默认值，可调整）

| # | 决策点 | 默认选择 | 说明 |
|---|---|---|---|
| D1 | 项目存放位置 | **方案 A：`vault/projects/` 内** | 纯文件、随 vault 备份；外部路径映射（方案 B）留作二期扩展 |
| D2 | 删除策略 | **进系统回收站**（`trash` crate） | 二次确认后删除；`permanent` 参数可选物理删除 |
| D3 | 归档形态 | **移动到 `projects/archive/`** | 无元数据文件，归档=真实移动，还原=移回 |
| D4 | 一期文件操作 | **只读浏览 + 打开**（新建/删除文件二期） | 项目文件夹在资源管理器里自由管理，应用只做"入口" |
| D5 | 右键菜单 | **二期再做** | 一期：卡片操作按钮 + 双击打开 |

> 若 D1~D5 需要调整，告诉我哪条，按新决策改设计。

---

## 1. 需求与交互形态

### 1.1 需求
- **项目** = 一个真实文件夹（工作文件：文档/表格/图片/压缩包…），在应用内可视化
- **归档** = 完成的项目统一收纳，与进行中分开
- **打开** = 点击文件 → 系统默认应用；点击文件夹 → 资源管理器（"和在文件夹时一样"）

### 1.2 页面形态

**① 项目列表页**

```
┌──────────────────────────────────────────────┐
│ 项目                            [+ 新建项目]  │
│                                              │
│ ┌ 进行中（2）───────────────────────────────┐ │
│ │ 📁 网站重构        12 个文件   归档 · 删除  │ │
│ │ 📁 客户报价        8 个文件    归档 · 删除  │ │
│ └───────────────────────────────────────────┘ │
│ ┌ 已归档（1）───────────────────────────────┐ │
│ │ 📁 2025 年报        已归档    还原 · 删除   │ │
│ └───────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

**② 项目详情页（文件浏览器）**

```
┌──────────────────────────────────────────────┐
│ ← 全部项目   网站重构                        │
│ 位置：D:\vault\projects\网站重构              │
│ ┌──────────────────────────────────────────┐ │
│ │ 📁 设计稿        打开文件夹                 │ │
│ │ 📁 代码          打开文件夹                 │ │
│ │ 📄 需求文档.docx  6.2 KB    双击→Word       │ │
│ │ 📄 排期.xlsx      3.1 KB    双击→Excel      │ │
│ └──────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

---

## 2. 目录结构（纯文件优先）

```
vault/
├── notes/                    # 笔记（已有）
├── projects/                 # ★ 项目文件管理
│   ├── 网站重构/             # 进行中项目 = 普通文件夹
│   │   ├── 设计稿/ 代码/ 需求文档.docx …
│   ├── 客户报价/
│   └── archive/              # 归档区
│       └── 2025 年报/
└── …
```

- **无索引/元数据文件**：项目列表 = `projects/` 与 `projects/archive/` 的直接子目录
- 归档重名：`archive/项目名-2`、`-3` 自动递增
- 文件树：仅浏览，不隐藏点文件（项目文件是用户自己的，不按笔记规则过滤）

---

## 3. Rust API 设计（`src-tauri/src/core/projects.rs`）

### 3.1 命令签名

```rust
/// 项目列表：进行中 + 已归档（各带文件数）
#[tauri::command]
pub async fn projects_list(vault: String) -> Result<Vec<ProjectInfo>, String>
// ProjectInfo { name: String, archived: bool, file_count: usize }

/// 创建项目文件夹 projects/<name>/
#[tauri::command]
pub async fn projects_create(vault: String, name: String) -> Result<(), String>

/// 归档：projects/<name>/ → projects/archive/<name>/（重名自动加 -2/-3）
#[tauri::command]
pub async fn projects_archive(vault: String, name: String) -> Result<(), String>

/// 还原：projects/archive/<name>/ → projects/<name>/
#[tauri::command]
pub async fn projects_unarchive(vault: String, name: String) -> Result<(), String>

/// 删除项目（默认进系统回收站；permanent=true 物理删除）
#[tauri::command]
pub async fn projects_delete(vault: String, name: String, permanent: bool) -> Result<(), String>

/// 项目内文件列表（一层；dir 相对项目根，"" = 根目录）
#[tauri::command]
pub async fn projects_files(vault: String, name: String, dir: String) -> Result<Vec<ProjectFile>, String>
// ProjectFile { name, path(相对项目根,/分隔), is_dir, size: Option<u64> }

/// 用默认应用打开文件 / 资源管理器打开文件夹
#[tauri::command]
pub async fn projects_open(vault: String, name: String, rel: String) -> Result<(), String>
```

### 3.2 安全与校验（复用 `core::path` 思路）

- `validate_project_name(name)`：
  - trim 后非空，长度 ≤ 128
  - 禁止字符：`/ \ : * ? " < > |`（Windows 保留）及控制字符
  - 禁止 `.` `..` 开头（隐藏目录 / 越界）
- `resolve_in_project(vault, name, rel)`：
  - `projects/<name>/` 为根，`rel` 必须解析后仍落在根内（复用 canonical-parent 检查）
  - 拒绝 `rel` 为空 / `.` / `..` / 绝对路径
- `projects_open` 的 `rel` 同规则；**只允许打开，不允许写入/删除项目内文件**（一期）

### 3.3 错误信息约定

| 场景 | 错误文案 |
|---|---|
| 项目名非法 | `项目名不合法: <name>` |
| 项目不存在 | `项目不存在: <name>` |
| 重名创建 | `项目已存在: <name>` |
| 归档重名 | 自动加后缀（不报错） |
| 打开文件不存在 | `文件不存在: <rel>` |
| 越界 | `路径越界` |

### 3.4 依赖

```toml
# src-tauri/Cargo.toml 新增
trash = "5"        # 回收站删除（Windows/macOS/Linux 统一）
```
- 打开文件用**已有** `tauri-plugin-opener`（`open_path`），capabilities 已含 `opener:default`，无需新增权限

---

## 4. 前端设计

### 4.1 状态层（`src/core/projects.tsx`）

```ts
interface ProjectInfo { name: string; archived: boolean; fileCount: number }
interface ProjectFile { name: string; path: string; isDir: boolean; size: number | null }

interface ProjectContextValue {
  projects: ProjectInfo[];        // 全部项目（含归档）
  loading: boolean;
  refresh: () => Promise<void>;
  create: (name: string) => Promise<void>;
  archive: (name: string) => Promise<void>;
  unarchive: (name: string) => Promise<void>;
  remove: (name: string) => Promise<void>;
  // 详情页状态
  current: string | null;         // 当前打开的项目名
  cwd: string;                    // 当前目录（相对项目根）
  files: ProjectFile[] | null;
  openProject: (name: string) => Promise<void>;
  enterDir: (rel: string) => Promise<void>;
  backDir: () => void;            // 面包屑回退
  openFile: (rel: string) => Promise<void>;   // 默认应用打开
}
```

- 列表与文件浏览各走一次 invoke，不做本地缓存副本（文件为真源）
- 复用 `useVault().path` 拿工作区路径；无工作区时项目页显示引导

### 4.2 组件树

```
ProjectsView
├── ProjectListPage（current === null 时）
│   ├── header：标题 + 新建输入（回车创建）
│   ├── section 进行中：ProjectCard[]
│   └── section 已归档：ProjectCard[]（按钮变"还原"）
└── ProjectDetailPage（current 非空时）
    ├── 顶栏：←全部项目 / 项目名 / 真实路径
    ├── 面包屑：根 ▸ 子目录 ▸ …
    └── FileList
        ├── 文件夹行：📁 名称（点击进入；行尾按钮"在资源管理器打开"）
        └── 文件行：📄 名称 + 大小（点击 → openFile）
```

### 4.3 交互细节

| 操作 | 行为 |
|---|---|
| 新建项目 | 输入名回车 → `create` → 刷新 → 状态栏提示；空名/非法名显示错误 |
| 归档 / 还原 | 直接执行 + 状态栏提示（不弹确认，可撤销=还原） |
| 删除 | `confirm("删除项目 X？将移入回收站")` → `remove` |
| 打开文件 | `openFile(rel)` → 系统默认应用；失败（文件被删）→ 错误提示 + 刷新列表 |
| 进入文件夹 | `enterDir(rel)` → 加载子目录；面包屑可逐级返回 |
| 打开文件夹 | 行尾按钮 → 资源管理器打开该目录 |

### 4.4 导航集成

- `Sidebar`：工作区组新增 `{ id: "projects", label: "项目", icon: IconFolder }`
- `App.tsx`：view switch 增加 `projects` 分支 → `<ProjectsView />`；`NavProvider` 无需扩展（视图内自管）

### 4.5 样式

- 复用现有卡片/按钮/徽章体系（`.project-card`、`.project-actions` 少量新增）
- 文件列表样式参考 `.checklist-list`（紧凑行 + hover 操作）

---

## 5. 测试计划

### 5.1 Rust 单测（`projects.rs` tests 模块）

| 用例 | 断言 |
|---|---|
| `name_validation` | 空名 / `..` / 斜杠 / 保留字符 → 拒绝 |
| `create_and_list` | create → list 含该项，file_count 正确 |
| `archive_roundtrip` | archive → 移入 archive/；unarchive → 移回 |
| `archive_name_collision` | 同名归档 → 自动 `-2` 后缀 |
| `path_escape_rejected` | rel 含 `..` / 绝对路径 → 越界错误 |
| `files_lists_dir` | 项目内文件/子目录/大小正确 |
| `delete_permanent` | permanent=true 物理删除（回收站分支不单测，靠 E2E） |

### 5.2 真实 E2E（CDP 9226，`scripts/cdp-projects.mjs`）

1. 新建项目「测试项目」→ 列表出现
2. 归档 → 移入"已归档"；还原 → 回到"进行中"
3. 进入详情 → 文件列表渲染（含子目录）
4. 点击文件 → 验证 `projects_open` 调用成功（不阻塞等应用弹出）
5. 越界路径（如 rel=`../x`）→ 前端收到错误提示

### 5.3 回归
- 全部现有测试（16/16）+ `pnpm build`（tsc）
- 确认不影响笔记/博客/插件（`projects/` 不在 notes/ 扫描范围，`resolve_safe` 隔离）

---

## 6. 实施任务拆分

### M-P1：命令与列表页
- [ ] `projects.rs`：validate / resolve / 7 个命令 + 7 组单测
- [ ] `api.ts`：7 个 invoke 封装
- [ ] `src/core/projects.tsx`：ProjectProvider（列表 CRUD）
- [ ] `ProjectsView` 列表页（新建/归档/还原/删除卡片）+ Sidebar/App 接入
- [ ] 验证：cargo test + build + E2E 场景 1/2

### M-P2：文件浏览器
- [ ] `projects_files` / `projects_open` 命令（M-P1 已含实现，此阶段接前端）
- [ ] 详情页 + 文件列表 + 面包屑 + 打开文件/文件夹
- [ ] 验证：E2E 场景 3/4/5

### M-P3：打磨与文档
- [ ] 文件类型图标（按扩展名）、大小格式化（KB/MB）
- [ ] 删除确认框、空状态引导、项目名非法提示样式
- [ ] 更新 `docs/操作手册.md`（新增项目页使用说明）
- [ ] 全量回归 + 提交

---

## 7. 风险与对策

| 风险 | 说明 | 对策 |
|---|---|---|
| 项目名非法/越界 | 破坏目录结构 | Rust 强校验 + 单测 |
| 归档重名 | 覆盖已有项目 | 自动数字后缀 |
| 删除误操作 | 不可恢复 | 默认回收站 + 二次确认 |
| 大项目枚举卡 UI | 文件很多 | 命令 async + 一层层按需加载 |
| trash crate 平台差异 | 某些环境回收站失败 | 失败时回退提示"改为永久删除？" |
| 双击文件误开危险文件 | exe 等被执行 | 一期仅提示打开，不自动运行未知类型（open_path 交由系统处理，系统本身有确认机制） |

---

## 8. 开放问题（可后续决定）

1. 是否要项目「描述/标签」（需要引入 `projects/.meta/*.json` 元数据）——一期不加，保持纯文件夹
2. 二期扩展：外部路径映射（D1-B）、项目内新建/删除/重命名文件、拖入文件、右键菜单
3. 是否与「记录」功能打通（项目 ↔ 工作记录关联）——等记录功能去留确定后再议
