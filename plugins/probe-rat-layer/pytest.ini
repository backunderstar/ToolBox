[pytest]
testpaths = test/probe_tests
addopts = -p no:cacheprovider --basetemp=D:/WORKSPACE/ToolBox/plugins/probe-rat-layer/test/.pytest_tmp
# vendor/ 里有 matplotlib/numpy 自带的测试目录，必须排除（否则 pytest 会尝试收集它们）
norecursedirs = vendor env .venv node_modules out jobs
