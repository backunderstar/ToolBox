[package]
name = "tb-sdk"
version = "0.4.2"
description = "ToolBox 核心插件 SDK：宿主 ↔ 插件 C ABI 契约与开发样板"
edition = "2021"

# 继承 workspace lints（linker_messages 放行等，见根 Cargo.toml [workspace.lints]）
[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
